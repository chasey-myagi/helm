{
  "name": "helm-board",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "A live, steerable single-task work board for coding agents.",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.browser.json --noEmit",
    "check": "npm run typecheck && node build.mjs --check"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  }
}
